# Docker Compose for Agrovee Backend
# Includes PostgreSQL, Redis, and Backend API

version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:14-alpine
    container_name: agrovee_db
    environment:
      POSTGRES_USER: agrovee
      POSTGRES_PASSWORD: agrovee123
      POSTGRES_DB: agrovee_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agrovee"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - agrovee_network

  # Redis Cache (optional)
  redis:
    image: redis:7-alpine
    container_name: agrovee_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - agrovee_network

  # FastAPI Backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agrovee_backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://agrovee:agrovee123@postgres:5432/agrovee_db
      - REDIS_URL=redis://redis:6379/0
      - ENVIRONMENT=development
      - DEBUG=True
    volumes:
      - ./app:/app/app
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ../p1/models:/app/models  # Mount trained model
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - agrovee_network
    command: >
      sh -c "
        python scripts/init_db.py &&
        uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      "

volumes:
  postgres_data:
  redis_data:

networks:
  agrovee_network:
    driver: bridge
